// conda.enabled = true
// conda.useMamba = true
// process.executor = 'slurm' 

// process {
//     executor = 'slurm'
//     clusterOptions = '--account mutationalscanning -p normal -c 1 --mem=4g -t 00:00:10'
// }

// use local execution
// process.executor = 'local'